name: Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    name: Ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - run: pip install ruff
      - run: ruff check custom_components tests
      - run: ruff format --check custom_components tests

  unit:
    name: Domaenentests (Stufe 1)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - run: pip install -r requirements-dev.txt
      - run: pytest tests/unit -m "not hass"

  integration:
    name: Home-Assistant-Tests (Stufe 2)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      # Home Assistant 2026.8 setzt Python 3.14.2 voraus.
      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
      - run: pip install -r requirements-test.txt
      # Die Ausgabe wird zusaetzlich als Annotation gemeldet: Job-Logs sind nur
      # mit Admin-Rechten abrufbar, Annotationen dagegen oeffentlich lesbar.
      - name: Integrationstests
        run: |
          if pytest tests/integration -q --tb=line -rf > pytest.log 2>&1; then
            cat pytest.log
          else
            cat pytest.log
            # Gezielt die Zusammenfassung melden; der rohe Mitschnitt besteht
            # groesstenteils aus Home-Assistant-Logzeilen.
            {
              grep -E '^E  ' pytest.log | sort -u | head -n 15
              sed -n '/short test summary info/,$p' pytest.log
            } | head -n 40 | while IFS= read -r zeile; do
              echo "::error::$zeile"
            done
            exit 1
          fi

  hassfest:
    name: Hassfest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: home-assistant/actions/hassfest@master

  hacs:
    name: HACS-Validierung
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: hacs/action@main
        with:
          category: integration
          # Die Marken-Pruefung verlangt einen Eintrag in home-assistant/brands,
          # den es fuer eine neue eigene Integration nicht gibt.
          ignore: brands
